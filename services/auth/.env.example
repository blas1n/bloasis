# Auth Service Configuration
# Copy this file to .env and configure values

# Service identity (gRPC only - no HTTP port needed, Kong handles HTTP)
SERVICE_NAME=auth
GRPC_PORT=50059

# Redis configuration
REDIS_HOST=redis
REDIS_PORT=6379

# User Service configuration
USER_SERVICE_HOST=user
USER_SERVICE_PORT=50058

# =============================================================================
# JWT Configuration
# =============================================================================
# Two algorithms are supported:
#
# RS256 (recommended for microservices):
#   - Uses asymmetric RSA keys
#   - Private key stays in Auth Service (signing)
#   - Public key distributed to Kong (verification)
#   - Generate keys with: ./scripts/generate_jwt_keys.sh
#
# HS256 (legacy, for backward compatibility):
#   - Uses symmetric shared secret
#   - Same secret must be in Auth Service and Kong

# Choose algorithm (RS256 or HS256)
JWT_ALGORITHM=RS256

# RS256 configuration (used when JWT_ALGORITHM=RS256)
JWT_PRIVATE_KEY_PATH=infra/keys/jwt-private.pem
JWT_PUBLIC_KEY_PATH=infra/keys/jwt-public.pem

# HS256 configuration (used when JWT_ALGORITHM=HS256)
# JWT_SECRET_KEY=your_jwt_secret_key_here  # Generate with: openssl rand -base64 32

# Token expiry settings
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# =============================================================================
# Consul configuration
CONSUL_HOST=consul
CONSUL_PORT=8500
CONSUL_ENABLED=true

# Logging
LOG_LEVEL=INFO
